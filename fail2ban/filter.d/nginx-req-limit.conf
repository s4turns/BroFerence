[Definition]
# Match nginx rate limit rejections (requires limit_req in nginx config)
failregex = ^\S+ \S+ - \S+ \[.*\] ".*" 429 .* "<HOST>".*$
            ^.*\[error\].*limiting requests.*client: <HOST>.*$

ignoreregex =
