[Definition]
# Match TURN auth failures: error 401 Unauthorized on ALLOCATE requests
failregex = ^.*: session .*: incoming packet ALLOCATE processed, error 401: Unauthorized.*client <HOST>.*$
            ^.*: session .*: realm .* user .*: incoming packet .* processed, error 4\d\d:.*client <HOST>.*$

ignoreregex =
